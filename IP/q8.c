#include <stdio.h>
#include <math.h>

int main(void) {

    float x, y;

    printf("Digite dois valores:\n");
    scanf("%f %f", &x, &y);

    if(x > y) {

        printf("O maior numero eh %f\n", x);

    }
    if(y > x) {

            printf("O maior numero eh %f\n", y);

    } else {
        if(x = y) {

            printf("Os valores sao iguais\n");

            }
        }

    return 0;

}
